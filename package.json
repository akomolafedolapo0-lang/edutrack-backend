{
  "name": "edutrack-ai-backend",
  "version": "1.0.0",
  "description": "Backend proxy server for EduTrack AI — keeps the Anthropic API key off the client.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
